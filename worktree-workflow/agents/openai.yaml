interface:
  display_name: "Worktree Workflow"
  short_description: "Coordinate and verify safe multi-worktree development"
  default_prompt: "Use $worktree-workflow to coordinate repository worktree locks and apply the project's multi-worktree workflow."
